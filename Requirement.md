# 待办

## 机制

- [ ] 映射镜: 复制物体至对称位置，并改变其状态或机制

## 关卡

- [X] 关卡数据文件: json格式，加载关卡时读取。
- [ ] 关卡剧情事件化：用事件系统重写关卡剧情
  - [X] 区域触发事件化
  - [X] 关卡 1
  - [X] 关卡 2
  - [X] 关卡 3
  - [ ] 关卡 5
  - [ ] 关卡 6
  - [ ] 关卡 8
- [ ] 修复：暂停时关卡信息不会隐藏
- [ ] 修复：暂停和重新开始引发的异步问题
- [ ] 修复：关卡信息可能会和各种UI重合
- [ ] 修复：对话气泡可能会和各种UI重合

## 美术

- [ ] 地砖：Shader计算光照与阴影
  - [X] 花纹部分的光影与阴影
  - [ ] 非花纹部分的光影与阴影
  - [ ] 修复：非花纹部分的光影会有细微偏移，进而影响花纹部分的视觉效果

## 优化

- [ ] 音量设置: 可调整音乐、音效声音大小
- [ ] 按键设置: 支持修改绑定的按键
- [ ] 多语言设置: 支持英文

---

# 设计备忘

## 镜子机制

- 复制玩家: 碰撞层1、2层反转
- 复制墙体: 虚假值反转
- 复制按钮: 虚假值反转
- 复制炸弹: 虚假值反转
- 复制线索: 乱码恢复或变乱码
- 复制出口: 无特殊
- 复制镜子: 不允许

## 关卡数据兼容性

- 当解析发生变化时，关卡数据需更新，并于更新日志中添加变更记录！！！

## 关卡数据兼容性

- 当解析发生变化时，关卡数据需更新，并于更新日志中添加变更记录！！！

## 关卡数据格式

- `必要字段`: 在此文档中用这个格式显示，以下这样的字段在文件中必须存在
- #comment: 于json中作为注释
- #comment: 以下只定义在json中的格式
- #comment: 对于用于实例化场景的键，若无则不实例化
- #comment: 对于实例化场景的属性，若无则用实例默认值
- `格式版本`: 整数或二维向量或二维数组，二维表示可兼容的版本范围
- `元数据`: 对象

  - `id`: 字符串，关卡的唯一标识
  - name: 字符串，关卡名，若无则以id为默认名
  - description: 字符串，关卡描述
- `地图`: 对象

  - `cell_size`: 对象

    - `width`: 整数，单位为像素，单元坐标宽度兼瓦片宽度
    - `height`: 整数，单位为像素，单元坐标高度兼瓦片高度
  - floor: 对象，用于实例化 floor

    - X: 整数，原点在地图中的横坐标
    - Y: 整数，原点在地图中的纵坐标
    - W: 整数，横轴方向上的瓦片数量
    - H: 整数，纵轴方向上的瓦片数量
  - external_wall: 对象数组，用于实例化 external_wall，同行同列的相邻对象之间会相连，以下为对象字段

    - `二维坐标`
    - 允许相连的方向：对象
      - N: 布尔值，Y负方向
      - S: 布尔值，Y正方向
      - W: 布尔值，X负方向
      - E: 布尔值，X正方向
  - exit: 对象，用于实例化 exit

    - `二维坐标`
    - `朝向`: 字符串，可为以下值
      - N: Y负方向，即旋转-90度
      - S: Y正方向，即旋转90度
      - W: X负方向，即旋转180度
      - E: X正方向，即旋转0度
- items: 对象数组，以下为对象字段

  - `二维坐标`
  - `Class`/`class`: 字符串，表示物体类型，使用类标识符，大写优先，无大写再看小写的（用于兼容）
  - `buttons`: 对象数组，仅 ButtonController，以下为对象字段

    - color: 字符串，有则对应场景变体 ${Color}_button
    - tooltip_offset: 字符串，对应其属性
  - `walls`: 对象数组，仅 ButtonController，以下为对象字段

    - `朝向`: 字符串，注意伸缩的朝向是指变长时的，而非按钮on或off的。可为以下值
      - N: Y负方向， anchor 属性为(0.5,0)
      - S: Y正方向， anchor 属性为(0.5,1)
      - W: X负方向， anchor 属性为(1,0.5)
      - E: X正方向， anchor 属性为(0,0.5)
  - `size_on`: 二维向量，仅 ButtonController
  - `size_off`: 二维向量，仅 ButtonController
  - fake: 布尔值，仅 FakableObject，也适用于 ButtonController 中 buttons 或 walls 的对象
  - fakable: 布尔值，仅 FakableObject

## 性能红线

- 同屏 &lt; 200 物体，低端机60fps
- 新机制必须同时支持触屏+键鼠
